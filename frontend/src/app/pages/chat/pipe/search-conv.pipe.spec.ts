import { SearchConvPipe } from './search-conv.pipe';

describe('SearchConvPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchConvPipe();
    expect(pipe).toBeTruthy();
  });
});
