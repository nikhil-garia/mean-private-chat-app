import { CustomslicePipe } from './customslice.pipe';

describe('CustomslicePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomslicePipe();
    expect(pipe).toBeTruthy();
  });
});
