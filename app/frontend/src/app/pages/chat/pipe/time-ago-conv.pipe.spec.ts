import { TimeAgoConvPipe } from './time-ago-conv.pipe';

describe('TimeAgoConvPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAgoConvPipe();
    expect(pipe).toBeTruthy();
  });
});
