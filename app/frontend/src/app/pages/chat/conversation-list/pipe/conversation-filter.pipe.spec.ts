import { ConversationFilterPipe } from './conversation-filter.pipe';

describe('ConversationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConversationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
